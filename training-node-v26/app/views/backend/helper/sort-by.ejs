
<% 
sortByHelper = (user_name,time) => { 
      let  xhtml = `
        <span class="glyphicon glyphicon-user" ></span> ${user_name} <br>
        <span class="glyphicon glyphicon-time"></span> ${ moment(time).format(systemConfig.format_long_time) }`
   return xhtml;
}
    
%>

